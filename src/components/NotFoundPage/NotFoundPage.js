import React from 'react';
import NavBar from '../NavBar/NavBar';
import NotFoundPageElement from './NotFoundPageElement/NotFoundPageElement';

const NotFoundPage = () => {
    return (
        <div>
            <NavBar/>
            <NotFoundPageElement/>
        </div>
     );
}

export default NotFoundPage;