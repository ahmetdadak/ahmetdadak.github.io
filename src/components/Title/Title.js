import React from 'react';
import './Title.css'
const Title = (props) => {
    return (
        <div className="Title" style={{marginTop: props.marginTop}}>
            Developer
        </div>
     );
}

export default Title;